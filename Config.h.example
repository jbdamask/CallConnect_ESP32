// Device-specific params
const char *CLIENT_ID= "<device id>";
const char *AP_PASSWORD = "<access point password>";
const uint8_t NUMPIXELS1 = 10;

// IoT broker
const char *awsEndPoint = "<endpoint from aws iot console>";
const char *subscribeTopic = "<mqtt topic>"; 
const char *publishTopic = "<mqtt topic>";

// Sensible defaults
const long RESET_AFTER = 21600000;  // 6 hours
const long LONG_PRESS_DURATION = 5000; // Used for long button push to restart the ESP chip